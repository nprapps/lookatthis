<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for slug, lastmod in posts %}
    <url>
        <loc>{{ S3_BASE_URL }}/posts/{{ slug }}</loc>
        <lastmod>{{ lastmod }}</lastmod>
    </url>
    {% endfor %}
</urlset>
